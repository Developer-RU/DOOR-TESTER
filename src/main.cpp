#include "main.hpp"

/**
 * @brief 
 * 
 */
void setup() 
{
    pinMode(POS_GERCON, INPUT);     digitalWrite(POS_GERCON, LOW);
    pinMode(NEG_GERCON, INPUT);     digitalWrite(NEG_GERCON, LOW);

    pinMode(PIN_RELE, OUTPUT);      digitalWrite(PIN_RELE, LOW);
}

/**
 * @brief 
 * 
 */
void loop() 
{
    
    
}